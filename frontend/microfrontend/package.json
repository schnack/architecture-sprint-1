{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "host_microfrontend",
      "auth_microfrontend"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}